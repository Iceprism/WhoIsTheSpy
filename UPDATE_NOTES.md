# 游戏功能更新说明

## 更新时间
2026年2月11日

## 新增功能

### 1. 管理员可见玩家身份和词语
- **功能描述**: 管理员在玩家列表中可以直接看到每个玩家的身份（平民/卧底）和对应的词语
- **实现位置**: 
  - 前端: `public/js/game.js` - `updatePlayersList()` 函数
  - 后端: `ws_server.php` - `broadcastPlayers()` 函数
  - 样式: `public/css/style.css` - 新增 `.player-role` 和 `.player-word` 样式

### 2. 自动倒计时功能（全局可见）
- **功能描述**: 每位玩家发言时自动开启60秒倒计时，所有人都能看到剩余时间
- **倒计时时长**: 60秒（在 `ws_server.php` 中通过常量 `SPEAK_DURATION` 定义）
- **实现位置**:
  - 后端: 
    - `ws_server.php` - `startRoomCountdown()` 启动倒计时
    - `ws_server.php` - `stopRoomCountdown()` 停止倒计时
    - `src/RedisClient.php` - 新增倒计时相关方法
  - 前端:
    - `public/js/game.js` - 倒计时显示和本地更新
- **倒计时触发时机**:
  - 游戏开始时
  - 管理员手动切换发言人时
  - 淘汰玩家后重新开始发言时
  - 自动切换下一位发言人时

### 3. 自动切换发言人
- **功能描述**: 倒计时结束后自动切换到下一位存活的玩家
- **实现位置**: `ws_server.php` - `startRoomCountdown()` 函数中的定时器回调

### 4. 轮回一圈自动投票
- **功能描述**: 当所有人发言一圈后（回到第一个发言人），自动开启投票
- **实现位置**: 
  - `ws_server.php` - 在倒计时结束和手动切换发言人时检测
  - `src/RedisClient.php` - 新增 `getFirstSpeaker()` 和 `setFirstSpeaker()` 方法
- **检测逻辑**: 
  - 游戏开始时记录第一个发言人
  - 每次切换时检查下一位是否是第一个发言人
  - 如果是，则停止倒计时并自动开启投票

## 修改的文件列表

### 前端文件
1. **public/js/game.js**
   - 新增倒计时相关状态变量
   - 新增倒计时显示和更新函数
   - 修改玩家列表显示，支持显示身份和词语
   - 新增处理倒计时消息的逻辑

2. **public/css/style.css**
   - 新增 `.player-role` 样式（显示身份标签）
   - 新增 `.player-word` 样式（显示词语）

### 后端文件
3. **ws_server.php**
   - 新增 `$roomTimers` 全局变量管理倒计时
   - 新增 `SPEAK_DURATION` 常量定义发言时长
   - 新增 `startRoomCountdown()` 函数启动倒计时
   - 新增 `stopRoomCountdown()` 函数停止倒计时
   - 修改 `broadcastState()` 函数，包含倒计时和第一发言人信息
   - 修改 `broadcastPlayers()` 函数，包含身份和词语信息
   - 修改 `start` 事件处理，启动倒计时并记录第一发言人
   - 修改 `next` 事件处理，检测轮回并重启倒计时
   - 修改 `kill` 事件处理，淘汰后重启倒计时
   - 修改 `end` 事件处理，停止倒计时
   - 修改 `onClose` 事件，房间无人时停止倒计时

4. **src/RedisClient.php**
   - 新增 `getFirstSpeaker()` - 获取第一个发言人
   - 新增 `setFirstSpeaker()` - 设置第一个发言人
   - 新增 `getSpeakTime()` - 获取剩余发言时间
   - 新增 `setSpeakTime()` - 设置剩余发言时间
   - 新增 `decrementSpeakTime()` - 减少发言时间

## 使用说明

### 管理员操作
1. 以昵称 "admin" 进入房间
2. 点击"开始游戏"按钮，游戏自动开始倒计时
3. 在玩家列表中可以看到每个玩家的身份和词语（带颜色标识）
4. 可以手动点击"下一位发言"切换发言人（会重新开始60秒倒计时）
5. 当一轮发言结束后，自动进入投票环节
6. 投票后可淘汰玩家，淘汰后自动开始新一轮发言

### 玩家界面
1. 所有玩家都能看到当前发言人和剩余时间
2. 普通玩家看不到其他人的身份和词语（只有管理员可见）
3. 倒计时在屏幕顶部的发言人提示区显示

## 技术细节

### 倒计时实现
- 后端使用 Workerman 的 Timer 每秒执行一次
- 使用 Redis 存储倒计时状态，支持多进程
- 前端接收倒计时更新消息，本地同步显示
- 前端也有本地倒计时，确保流畅显示

### 轮回检测
- 游戏开始时记录第一个发言人座位号
- 每次切换时比较下一位是否等于第一位
- 匹配则触发投票，不匹配则继续发言

### 数据同步
- 所有状态变化通过 WebSocket 实时广播
- 新玩家连接时同步完整状态（包括倒计时）
- 房间无人时自动停止倒计时，释放资源

## 注意事项

1. **倒计时时长修改**: 如需修改发言时长，编辑 `ws_server.php` 中的 `SPEAK_DURATION` 常量
2. **重启服务器**: 修改后需要重启 WebSocket 服务器才能生效
3. **Redis 依赖**: 倒计时状态存储在 Redis，确保 Redis 服务正常运行
4. **多房间支持**: 每个房间独立倒计时，互不影响

## 保持不变的部分

- 技术栈: PHP + Workerman + Redis + WebSocket
- 原有的所有功能保持完整
- 数据库结构无变化
- 配置文件无变化
- 部署方式无变化
