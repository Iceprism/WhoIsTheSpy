# 谁是卧底游戏开发

**User:** 奶嘴偷走初吻（Prism） (prism254809@gmail.com)  
**Created:** 2/11/2026 16:19:17  
**Updated:** 2/11/2026 16:33:35  
**Exported:** 2/11/2026 16:34:45  
**Link:** [https://chatgpt.com/c/698c3b27-3734-8320-b0f9-7185e26a8c8b](https://chatgpt.com/c/698c3b27-3734-8320-b0f9-7185e26a8c8b)  

## Response:
很好，现在所有**不确定边界已经全部锁定**，可以形成一份**可以直接进入编码阶段的最终技术方案**。  
本系统本质是一个 **一次性运行的“局域实时工具”**，不是长期服务，所以我们刻意避免一切“工程化过度设计”。

下面是 **最终版实施设计文档（V2.0 / 可开发版）**。

* * *

一、系统定位（非常重要）
============

这是一个 **短生命周期工具系统**：

*   为一次聚会临时启动
*   同时支撑 ≤7 个房间
*   一局结束 → 整体服务关闭
*   不考虑长期数据、不考虑容灾、不考虑扩展
*   Redis 只作为“共享内存”
*   不做持久化，不写数据库

可以理解为：

> 用 Web 技术写的一个“带界面的主持控制台”。

* * *

二、整体技术架构（极简）
============

```
浏览器
   │
HTTP（进入页面 / 分配身份）
   │
PHP-FPM
   │
Redis（唯一状态源）
   │
Workerman WebSocket（状态广播）
```

系统只有 **一个状态中心：Redis**

所有逻辑都围绕 Redis Key 操作。

* * *

三、房间配置文件（唯一外部数据）
================

文件：`/config/rooms.json`

该文件在游戏开始前准备好，运行期间绝不修改。

格式（固定）
------

```json
{
  "A001": {
    "seats": [
      {"seat":1,"role":"平民","word":"苹果"},
      {"seat":2,"role":"平民","word":"苹果"},
      {"seat":3,"role":"卧底","word":"香蕉"},
      {"seat":4,"role":"平民","word":"苹果"},
      {"seat":5,"role":"平民","word":"苹果"},
      {"seat":6,"role":"平民","word":"苹果"},
      {"seat":7,"role":"平民","word":"苹果"},
      {"seat":8,"role":"卧底","word":"香蕉"},
      {"seat":9,"role":"平民","word":"苹果"},
      {"seat":10,"role":"平民","word":"苹果"}
    ]
  },

  "B001": { ... },
  "C001": { ... }
}
```

seat 是 **预定义资源**，系统绝不生成新 seat。

* * *

四、Redis 数据结构（最终定稿）
==================

房间状态
----

```
room:{id}:status
值：waiting | started | voting | ended
```

* * *

玩家表（谁坐哪个 seat）
--------------

```
room:{id}:players   (HASH)

key = seat
value =
{
  "name":"张三",
  "alive":1
}
```

* * *

姓名映射（用于重连校验）
------------

```
room:{id}:name_map  (HASH)

key = name
value = seat
```

用于保证：  
✔ 姓名唯一  
✔ 刷新恢复身份  
✔ 禁止改名混入

* * *

当前发言人
-----

```
room:{id}:speaker = seat
```

循环推进。

* * *

投票箱
---

```
room:{id}:votes (HASH)
seat → count
```

* * *

已投名单（防重复投票）
-----------

```
room:{id}:voted (SET)
name
```

* * *

聊天区
---

```
room:{id}:chat (LIST)
```

只保留最近100条：

```
LPUSH + LTRIM 0 99
```

* * *

五、HTTP 接口（只保留 1 个）
==================

系统只有一个 HTTP 行为：

进入房间 `/api/join.php`
--------------------

请求：

```json
POST /api/join.php
{
  "room":"A001",
  "name":"张三"
}
```

* * *

服务器处理逻辑（核心规则）
-------------

### ① 校验房间是否存在

从 `rooms.json` 查。

不存在直接拒绝。

* * *

### ② 判断是否管理员

```
name === "admin"
→ 进入管理员模式
→ 不占 seat
```

* * *

### ③ 检查是否已存在（处理刷新）

```
name_map 存在 → 直接返回旧 seat
```

* * *

### ④ 如果游戏已开始，拒绝新玩家

```
status != waiting
→ 禁止加入
```

* * *

### ⑤ 分配 seat（顺序找未使用）

写入：

```
players
name_map
```

* * *

返回数据（进入房间所需全部信息）
----------------

```json
{
  "seat":3,
  "role":"卧底",
  "word":"香蕉",
  "isAdmin":false
}
```

客户端之后 **不再请求任何 HTTP**。

全部走 WS。

* * *

六、WebSocket 协议设计（必须严格按这个实现）
===========================

连接地址：

```
ws://host:2346?room=A001&name=张三
```

WS 不再鉴权，只读取参数。

* * *

客户端发送消息类型（固定6种）
---------------

### 1️⃣ 聊天

```json
{"action":"chat","msg":"我觉得是水果"}
```

* * *

### 2️⃣ 管理员开始游戏

```json
{"action":"start"}
```

效果：

```
status → started
speaker → 最小 seat
广播全体
```

* * *

### 3️⃣ 下一位发言

```json
{"action":"next"}
```

服务器循环查找下一个 alive seat。

* * *

### 4️⃣ 开启投票

```json
{"action":"vote_start"}
```

执行：

```
status → voting
DEL votes
DEL voted
```

* * *

### 5️⃣ 玩家投票

```json
{"action":"vote","target":5}
```

服务器检查：

```
是否已投过（voted）
```

然后：

```
HINCRBY votes
SADD voted
```

* * *

### 6️⃣ 管理员淘汰玩家

```json
{"action":"kill","seat":5}
```

只做：

```
alive = 0
```

不做任何输赢判断。

* * *

### 7️⃣ 管理员结束本局

```json
{"action":"end"}
```

执行：

```
status → ended
房间锁死
```

该房间不能再使用。

* * *

七、服务器广播事件（客户端只处理这些）
===================

服务器只广播三类消息：

房间状态同步
------

```json
{"type":"state","status":"started","speaker":3}
```

* * *

玩家列表变化
------

```json
{"type":"players","list":[...]}
```

* * *

聊天更新
----

```json
{"type":"chat","msg":{...}}
```

* * *

投票更新
----

```json
{"type":"votes","data":{seat:count}}
```

* * *

八、页面功能（严格限制）
============

玩家页面只允许：
--------

*   看自己身份
*   看当前谁发言
*   自由聊天（序号-姓名-时间）
*   投票（投票阶段才出现按钮）

* * *

管理员页面多出控制按钮：
------------

*   开始游戏
*   下一位
*   开启投票
*   淘汰 seat
*   结束本局

* * *

九、完整游戏流程（系统执行顺序）
================

```
启动 Redis
启动 WS
打开网页
玩家进入
管理员点击【开始游戏】

循环：
  发言推进
  开启投票
  玩家投票
  管理员判定淘汰

管理员点击【结束本局】

关闭服务
```

没有自动逻辑，没有胜负判断。

* * *

十、代码规模预估（真实工作量）
===============

| 模块 | 代码量 |
| --- | --- |
| join接口 | ~150行 |
| WS服务 | ~400行 |
| Redis封装 | ~150行 |
| 前端JS | ~300行 |

总计 ≈ 1000 行

* * *

十一、部署方式（一次性运行）
==============

启动顺序：

```
redis-server
php-fpm
php ws_server.php start
```

结束游戏后：

```
CTRL+C 关闭 WS
关闭 Redis
```

不会留下任何数据。

* * *

十二、这个设计为什么稳定
============

因为它本质不是“游戏服务器”，而是：

✔ Redis = 内存共享表  
✔ WS = 广播器  
✔ PHP = 发身份脚本

没有复杂状态，没有数据库事务，没有并发锁问题。

* * *

下一步（真正进入开发）
===========

如果你确认这版没有问题，下一步我将直接给出：

1️⃣ 目录结构初始化  
2️⃣ `rooms.json` 示例  
3️⃣ `join.php` 完整代码设计  
4️⃣ WS Server 主循环设计

也就是正式开始写代码了。



---
Powered by [ChatGPT Exporter](https://www.chatgptexporter.com)