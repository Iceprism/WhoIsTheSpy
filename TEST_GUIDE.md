# 功能测试指南

## 测试前准备

1. 确保 Redis 服务正常运行
2. 重启 WebSocket 服务器（重要！）
   ```bash
   # Windows PowerShell
   php stop.bat
   php start.bat
   ```

## 测试步骤

### 测试1: 管理员可见身份和词语

1. 打开浏览器访问游戏
2. 以昵称 `admin` 加入房间（如 A001）
3. 让其他玩家（至少3人）以普通昵称加入
4. **预期结果**: 
   - 管理员在"玩家列表"中能看到每个玩家下方显示的身份（平民/卧底）
   - 能看到对应的词语
   - 平民显示绿色背景，卧底显示红色背景

### 测试2: 自动倒计时（全局可见）

1. 管理员点击"开始游戏"
2. **预期结果**:
   - 顶部显示"🎤 当前发言人: X号 XXX (60秒)"
   - 倒计时从60秒开始递减
   - **所有玩家**（包括管理员和普通玩家）都能看到相同的倒计时
   - 每秒更新一次

### 测试3: 自动切换下一位发言人

1. 等待倒计时归零（60秒）或者手动点击"下一位发言"
2. **预期结果**:
   - 倒计时归零后，自动切换到下一个存活玩家
   - 倒计时重新从60秒开始
   - 聊天区显示系统消息："请 X号 XXX 发言"
   - 玩家列表中当前发言人高亮显示（黄色背景）

### 测试4: 手动切换发言人

1. 管理员在倒计时未结束时点击"下一位发言"
2. **预期结果**:
   - 立即切换到下一位
   - 倒计时重新从60秒开始
   - 不影响轮回计数

### 测试5: 轮回一圈自动投票

1. 等待所有玩家依次发言
2. 当轮到第一个发言人再次发言时
3. **预期结果**:
   - 倒计时停止
   - 自动进入投票状态
   - 聊天区显示："所有人发言完毕，开始投票！"
   - 普通玩家看到投票界面
   - 管理员看到投票统计

### 测试6: 淘汰后重新开始

1. 投票结束后，管理员淘汰一名玩家
2. **预期结果**:
   - 被淘汰的玩家显示为灰色，划掉
   - 自动设置下一个发言人
   - 重新记录第一个发言人（用于下一轮检测）
   - 倒计时重新开始
   - 游戏状态恢复为"游戏中"

### 测试7: 结束游戏

1. 管理员点击"结束本局"
2. **预期结果**:
   - 倒计时停止
   - 游戏状态变为"已结束"
   - 所有控制按钮禁用

## 常见问题排查

### 问题1: 看不到身份和词语
- 确认是以 `admin` 昵称登录
- 检查房间配置文件中是否有正确的身份和词语设置

### 问题2: 倒计时不显示
- 检查 WebSocket 服务器是否正常运行
- 查看浏览器控制台是否有 WebSocket 连接错误
- 确认 Redis 服务正常

### 问题3: 倒计时归零但不自动切换
- 检查 WebSocket 服务器日志
- 确认 Workerman Timer 功能正常
- 重启 WebSocket 服务器

### 问题4: 不会自动投票
- 确认已经轮回完一圈
- 检查第一个发言人记录是否正确
- 查看服务器日志中的发言人切换记录

## 调试建议

1. **查看服务器日志**: 
   - 服务器会输出每次发言人切换的信息
   - 可以看到倒计时的执行情况

2. **浏览器开发者工具**:
   - 打开 Network 标签，查看 WebSocket 消息
   - 可以看到倒计时的实时广播

3. **检查 Redis 数据**:
   ```bash
   redis-cli
   keys room:*
   get room:A001:speak_time
   get room:A001:first_speaker
   ```

## 性能说明

- 倒计时每秒广播一次，对服务器压力很小
- 每个房间独立倒计时，不会互相影响
- 房间无人时自动停止倒计时，节省资源
